import { MealPlanWeekView } from './meal-plan-week-view';

// Expo Router requires [id].tsx for dynamic routes
// This is a thin wrapper that delegates to the main component
export default MealPlanWeekView;
